import 'package:flutter/material.dart';

class Mario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(

          // child: removeDiacritics('arïó');
          ),
    );
  }
}
